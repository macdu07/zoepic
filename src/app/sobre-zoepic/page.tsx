import type { Metadata } from "next";
import Link from "next/link";
import { JsonLd } from "@/components/seo/JsonLd";
import { PublicShell } from "@/components/site/PublicShell";
import { aboutPageSchema, breadcrumbSchema, organizationSchema, SITE_URL, websiteSchema } from "@/lib/seo";

export const metadata: Metadata = { title: "Imágenes para la web: sobre ZoePic", description: "Conoce ZoePic, una herramienta para convertir, dimensionar y organizar imágenes antes de publicarlas en la web.", alternates: { canonical: "/sobre-zoepic" }, openGraph: { url: "/sobre-zoepic" } };
export default function AboutPage() { const schema = { "@context": "https://schema.org", "@graph": [organizationSchema(), websiteSchema(), aboutPageSchema(), breadcrumbSchema([{ name: "Inicio", url: SITE_URL }, { name: "Sobre ZoePic", url: `${SITE_URL}/sobre-zoepic` }])] }; return <PublicShell><JsonLd data={schema} /><main className="reading px-5 py-16 sm:px-6 md:py-24"><h1 className="font-display text-5xl font-medium tracking-[-.03em]">Sobre ZoePic y la preparación de imágenes web.</h1><div className="mt-10 space-y-8 text-lg leading-8 text-foreground/80"><p>ZoePic nace para reunir tareas que suelen quedar repartidas entre editores distintos: convertir una imagen, ajustar sus dimensiones, revisar el peso y preparar un nombre entendible. Su foco son profesionales que publican en sitios web, tiendas y bibliotecas de contenido.</p><p>La conversión a WebP y el cambio de tamaño se ejecutan en el navegador. Esto reduce el recorrido de los archivos y permite empezar sin una cuenta. El renombrado con IA es opcional y utiliza un servicio externo; la interfaz lo distingue para que el usuario pueda decidir.</p><p>No existe un porcentaje de ahorro garantizado para todas las imágenes. ZoePic muestra los valores reales de cada resultado para que la decisión se tome con evidencia. Las guías explican las limitaciones del formato y complementan la herramienta.</p><p>El producto conserva un modelo gratuito con límites y planes profesionales para flujos de mayor volumen o renombrado asistido.</p><p>El servicio es mantenido por Mauricio Correa en Colombia. Para soporte, privacidad o dudas sobre el tratamiento de datos puedes escribir a <a href="mailto:privacy@zoepic.online">privacy@zoepic.online</a>.</p></div><Link href="/convert" className="mt-10 inline-flex rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Abrir conversor</Link></main></PublicShell>; }
